--- a/50_add_susfs_in_gki-android15-6.6.patch
+++ b/50_add_susfs_in_gki-android15-6.6.patch
@@ -661,18 +661,16 @@
 index 2d0231797d0d..f27adac5ee5b 100644
 --- a/fs/namespace.c
 +++ b/fs/namespace.c
-@@ -32,12 +32,37 @@
+@@ -32,10 +32,35 @@
+ #include <uapi/linux/mount.h>
  #include <linux/fs_context.h>
  #include <linux/shmem_fs.h>
- #include <linux/mnt_idmapping.h>
 +#if defined(CONFIG_KSU_SUSFS_SUS_MOUNT) || defined(CONFIG_KSU_SUSFS_TRY_UMOUNT)
 +#include <linux/susfs_def.h>
 +#endif
  
  #include "pnode.h"
  #include "internal.h"
- #include <trace/hooks/blk.h>
- #include <trace/hooks/fs.h>
  
 +#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 +extern bool susfs_is_current_ksu_domain(void);
@@ -697,7 +695,7 @@
 +#endif
 +
  /* Maximum number of mounts in a mount namespace */
- static unsigned int sysctl_mount_max __read_mostly = 100000;
+ unsigned int sysctl_mount_max __read_mostly = 100000;
  
 @@ -126,6 +151,19 @@ static inline struct hlist_head *mp_hash(struct dentry *dentry)
  	return &mountpoint_hashtable[tmp & mp_hash_mask];
@@ -853,9 +851,9 @@
  {
  	struct mount *mnt = kmem_cache_zalloc(mnt_cache, GFP_KERNEL);
 @@ -223,6 +365,10 @@ static struct mount *alloc_vfsmnt(const char *name)
- 		mnt->mnt_count = 1;
  		mnt->mnt_writers = 0;
  #endif
+ 		mnt->mnt.data = NULL;
 +#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 +		// Make sure mnt->mnt.susfs_mnt_id_backup is initialized every time.
 +		mnt->mnt.susfs_mnt_id_backup = 0;
@@ -864,8 +862,8 @@
  		INIT_HLIST_NODE(&mnt->mnt_hash);
  		INIT_LIST_HEAD(&mnt->mnt_child);
 @@ -1085,7 +1231,22 @@ struct vfsmount *vfs_create_mount(struct fs_context *fc)
- 	if (!fc->root)
  		return ERR_PTR(-EINVAL);
+ 	sb = fc->root->d_sb;
  
 +#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 +	// We won't check it anymore if boot-completed stage is triggered.
@@ -966,9 +964,9 @@
  }
  
 @@ -5060,3 +5271,37 @@ static int __init init_fs_namespace_sysctls(void)
- fs_initcall(init_fs_namespace_sysctls);
- 
- #endif /* CONFIG_SYSCTL */
+ 	.install	= mntns_install,
+ 	.owner		= mntns_owner,
+ };
 +
 +#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 +/* Reorder the mnt_id after all sus mounts are umounted during ksu_handle_setuid() */
